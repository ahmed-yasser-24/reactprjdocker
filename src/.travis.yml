# sudo: required
# language: generic

# services:
#  - docker

# before_install:
#  - docker build -t ahmedyasser/reactprjdocker -f Dockerfile.dev .

# script:
#  - docker run -e CI=TRUE ahmedyasser/reactprjdocker npm run test
